1. Create `src/components/simulations/CustomStatesOfMatter.jsx`.
2. Implement native Canvas loop.
3. Manage particles for Neon, Argon, Oxygen, Water.
4. Add Heat/Cool buttons.
5. Add Phase buttons (Solid, Liquid, Gas).
6. Update `physicsSimulations.json` to register `phys_14_mg` (States of Matter MG).
7. Update `PhysicsSimulationView.jsx` to route `phys_14_mg` to `CustomStatesOfMatter`.
8. Check if `dev` command builds properly.
